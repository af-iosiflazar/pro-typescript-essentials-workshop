/**
 * Adds two numbers.
 * @param a The first number.
 * @param b The second number.
 * @returns The sum of the the two numbers.
 */
const myFunction = (a: number, b: number) => {
  return a + b;
};
